{% extends 'base.html.twig' %}

{% block title %} Categories {%  endblock title %}

{% block body  %}
    <h1>Categories</h1>

    {% for category in categories %}

        <h2>{{ category.title }} - {{ category.color }}</h2>

    {% endfor%}

{% endblock body %}
